import pandas as pd

# Sample DataFrame
data = {'price': [11, 16, 1101]}
df = pd.DataFrame(data)

# Rounding to the nearest multiple of 10
df['rounded_price'] = df['price'].apply(lambda x: round(x, -1))

print(df)