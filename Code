uploader = SandraUploader(dbname="credit_qa")
    
    try:
        uploader.connect()  # Connect to the database
        message = uploader.upload_result(result=df, start_date=start_date, end_date=end_date)
        print(message)  # Print success message
    except Exception as e:
        print(f"Error during upload: {e}")
