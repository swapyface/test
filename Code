df['date_column'] = pd.to_datetime(df['date_column']).astype('datetime64[ms]')
import pandas as pd
import numpy as np

# Example: replace out-of-range dates with NaT (Not a Time)
df['date_column'] = pd.to_datetime(df['date_column'], errors='coerce')