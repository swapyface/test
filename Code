    min_value = np.min(original_category_data)
    max_value = np.max(original_category_data)
    
    # Calculate lower and upper bounds based on 20% down from min and 20% up from max
    lower_bound = min_value - 0.2 * min_value
    upper_bound = max_value + 0.2 * max_value
