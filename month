import pandas as pd

# Sample data
data = {'rating': ['A@', 'AA+', 'BBB-', 'C$', 'AAA!', 'BB#1']}
df = pd.DataFrame(data)

# Keep only letters, numbers, and the '-' symbol in the rating column
df['rating_cleaned'] = df['rating'].str.replace(r'[^a-zA-Z0-9-]', '', regex=True)

print(df)