from datetime import datetime

# Example ISO-formatted date
iso_date = "2024-12-03"

# Convert ISO string to a datetime object
dt = datetime.fromisoformat(iso_date)

# Format the datetime object to your desired format
formatted_date = f"{dt.year},{dt.month},{dt.day}"

print(formatted_date)  # Output: 2024,12,3