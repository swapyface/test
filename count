import pandas as pd

# Sample DataFrame
data = {'rating': ['-A', 'B+++', 'B-', 'C++', 'A']}
df = pd.DataFrame(data)

# Remove all non-alphabetic characters from the 'rating' column
df['cleaned_rating'] = df['rating'].str.replace(r'[^A-Za-z]', '', regex=True)

print(df)