import matplotlib.pyplot as plt
import seaborn as sns

# Sample DataFrame structure
# df['break_even_spread'] - Your target variable
# df['outlier'] - 0 for inliers, 1 for outliers

plt.figure(figsize=(10, 6))
sns.scatterplot(data=df, x=df.index, y="break_even_spread", hue="outlier", palette={0: "blue", 1: "red"}, alpha=0.7)

plt.xlabel("Index")
plt.ylabel("Break-Even Spread")
plt.title("Scatter Plot of Break-Even Spread (Inliers vs. Outliers)")
plt.legend(title="Outlier", labels=["Inlier", "Outlier"])
plt.show()